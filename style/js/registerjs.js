
var username          = $('#reg_username')[0].value;
var name              = $('#reg_name')[0].value;
var surname           = $('#reg_surname')[0].value;
var email             = $('#reg_email')[0].value;
var password          = $('#reg_password')[0].value;
var confirm_password  = $('#reg_confirm_password')[0].value;

username.blur(function() {
	if (!$(this).val().length === 0) {
		// $(this).addClass('error');
		// log err
	}
});
name.blur(function() {
	if (!$(this).val().length === 0) {
		// $(this).addClass('error');
		// log err
	}
});
surname.blur(function() {
	if (!$(this).val().length === 0) {
		// $(this).addClass('error');
		// log err
	}
});
email.blur(function() {
	if (!$(this).val().length === 0) {
		// $(this).addClass('error');
		// log err
	}
});
password.blur(function() {
	if (!$(this).val().length === 0) {
		// $(this).addClass('error');
		// log err
	}
});
confirm_password.blur(function() {
	if (!$(this).val().length === 0) {
		// $(this).addClass('error');
		// log err
	}
});